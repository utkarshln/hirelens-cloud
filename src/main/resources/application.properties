server.port=${PORT:8080}
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=200MB
spring.thymeleaf.cache=false
# For Railway: uses PORT env automatically; Postgres optional (in-memory ranking, no DB needed for MVP)
# To add Postgres: uncomment below and set DATABASE_URL
# spring.datasource.url=${DATABASE_URL:jdbc:h2:mem:hirelens}
management.endpoints.web.exposure.include=health,info
